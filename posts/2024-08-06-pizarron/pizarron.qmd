---
title: "Pizarrón"
subtitle: "para revealjs"
author: "David Schoch"
institute: "GESIS - Leibniz Institute for the Social Sciences"

image: "img/school-3258934_1280.jpg"

css: miguel-pz.css

format: blackboard-revealjs

revealjs-plugins:
  - pointer
  - attribution

---

```{r}
#| label: setup
#| include: false

library(showtext)
library(ggplot2)
font_add_google("Pangolin", "Pangolin")
showtext_auto()

# Define un tema (como una función) para lucir como de pizarra
# Utiliza la opción %+replace% para redefinir cada atributo de
# el tema "minimal" que es así usado como base.
theme_chalk <- function () { 
  theme_minimal() %+replace% 
    theme(
      axis.ticks = element_line(colour = "white", 
                                size = 0.25),
      text = element_text(colour = "white"),
      axis.text  = element_text(colour = "white",
                                family = "Pangolin", 
                                size = 18),
      axis.title  = element_text(colour = "white",
                                 family = "Pangolin", 
                                 size = 24),
      panel.background  = element_rect(colour = NA,
                                       fill="transparent"),
      plot.background = element_rect(colour = "white",
                                     fill = "transparent"),
      legend.position = "bottom",
      legend.title = element_blank(),
      panel.grid.minor = element_blank(),
      panel.grid.major.x = element_line(colour = "white", size=0.25),
      panel.grid.major.y = element_line(colour = "white", size=0.25),
      legend.text = element_text(size = 24)
    )
}

knitr::opts_chunk$set(dev.args=list(bg="transparent"))
```

## Estilo de _chunks_ y tema para _ggplot_

```{r}
#| label: testing
#| fig.showtext: true
#| fig.height: 4
#| fig.width: 12
#| fig.align: 'center'
#| echo: true
#| out-height: 100%

ggplot(cars) +
  geom_point(aes(speed, dist), col = "white") +
  theme_chalk()
```

## Imágenes y texto enmarcados

::: {.attribution}
**Imágen por:** Gernot from Pixabay
:::

:::{.framed}
!Puedes poner un marco a lo que quieras!\
usa la _fence_: `:::{.framed}`
:::

![](img/jaguar-3493552_1280-bw.png){.framed}

## Listas

- Hacer Listas

- es 

- divertido

  - La diversión puede ir más allá